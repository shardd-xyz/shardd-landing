# shardd landing

Static landing page and docs site for `shardd`, built with VitePress and meant
for GitHub Pages deployment.

## Local development

```bash
npm install
npm run docs:dev
```

## Build

```bash
npm run docs:build
```

The GitHub Pages workflow publishes the generated site from `docs/.vitepress/dist`.
